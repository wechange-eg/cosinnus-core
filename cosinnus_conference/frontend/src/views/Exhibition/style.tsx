import { Box} from "@material-ui/core"
import { styled } from "@material-ui/core/styles"

export const ExhibitionContainer = styled(Box)({
  height: "100%",
  display: "flex",
  flexDirection: "column"
})
